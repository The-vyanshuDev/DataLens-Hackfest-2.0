# API base URL (no trailing slash). Must point to your **backend** (e.g. Render).
# - Backend from render.yaml is named "datalens-api" → https://datalens-api.onrender.com
# - If you renamed the backend service on Render, use that service’s URL here.
# Leave unset for local dev (defaults to http://localhost:8000).
# VITE_API_BASE_URL=https://datalens-api.onrender.com
