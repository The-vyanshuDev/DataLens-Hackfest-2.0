# Required for business document generation (Step 4). Get a key from https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: comma-separated list of allowed frontend origins for CORS (e.g. production URL).
# Set in Render/Vercel to your frontend URL so the browser can call the API.
# CORS_ORIGINS=https://your-app.vercel.app
